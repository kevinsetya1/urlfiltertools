# urlfiltertools
  A Python tool that converts a list of URLs from an Excel file into wildcard-formatted domain entries, with automatic deduplication, IP &amp; port support, and grouped output — ready for use in firewall rules or proxy allowlists.
